## [2.0.0] - 2025-05-10

### 核心架构升级
- **基于关键词匹配的意图分类**：替代模型判断，采用确定性规则引擎，通过关键词映射表将用户输入分类为预设意图标签（查询/部署/排错/优化等），未命中时触发动态标签生成
- **复杂度评估体系**：1-10 分量化评估，综合考虑工具调用次数预估、跨系统依赖数、是否需要人工确认节点
- **任务分解引擎**：将复杂指令拆分为原子化步骤，每个步骤包含明确的工具调用序列和回退策略

### 联动机制
- **与 error-control 强制联动**：系统辨识结果自动注入误差控制框架，高复杂度任务（≥7分）强制启用多级重试和优雅降级
- **混排执行策略**：预设标签与动态生成标签混合编排，避免单一规则引擎的僵化问题

### 工程化改进
- 统一输出格式：YAML 结构化返回，包含 `intent`、`complexity`、`steps`、`risk_level`、`fallback` 字段
- 增加置信度评分：对每个识别结果附加 confidence score，低于阈值时自动请求用户澄清
- 支持上下文继承：多轮对话中保持意图连续性，避免重复分类开销

---

## [1.0.0] - 2025-04-26

### 初始版本
- 基于钱学森《工程控制论》第一章「引言」思想，将系统辨识概念转化为 AI 意图识别实践
- 实现三大核心功能：意图分类、复杂度评估、任务分解
- 混合触发机制：简单请求走快速路径，复杂请求走完整分析路径
- 四级复杂度体系：simple / medium / complex / critical
- 与 error-control 的协同接口：置信度 < 0.8 或 critical 复杂度时触发边界确认
- 动态标签生成：未命中预设标签时自动归纳新标签并持久化存储
